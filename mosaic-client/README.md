# Kaleidoscope Client

This package contains the React and Vite frontend for Kaleidoscope.

Useful commands:

```bash
npm run dev
npm run build
npm run lint
npm run check
npm run test -- --run
```

The app shell only exposes the preview workspace.